# Only needed for production deployment. Leave unset for local dev —
# Vite's dev server proxy (see vite.config.js) handles /api locally.
#
# Set this in your hosting platform's environment variables (e.g. Vercel)
# to your deployed backend's URL, including the /api path:
VITE_API_URL=https://your-backend-name.onrender.com/api
